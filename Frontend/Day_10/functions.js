
export function flipBackAll(){
  setTimeout(function(){
    {
      for(let index in card){
            if(card[item].classList.contains('is-flipped')){
            card[index].classList.toggle('is-flipped')
            counter=0;
            }
          }
        }
},3000)
}
